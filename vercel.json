{
  "version": 2,
  "installCommand": "cd frontend && npm install && chmod -R +x node_modules/.bin",
  "buildCommand": "cp -r backend api/backend && cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" }
  ]
}
